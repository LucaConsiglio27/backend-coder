<!DOCTYPE html>
<html>
<head>
    <title>Home</title>
</head>
<body>
    <h1>Product List</h1>
    <ul id="product-list">
        {{#each this}}
            <li>{{title}} - {{description}} - ${{price}}</li>
        {{/each}}
    </ul>
</body>
</html>
